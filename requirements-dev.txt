# Test-only dependencies. Install alongside requirements.txt and
# paperbook/requirements.txt to run the suite:
#
#     pip install -r requirements.txt -r paperbook/requirements.txt -r requirements-dev.txt
#     python -m pytest -q

pytest>=8.0

# starlette's TestClient imports httpx2, falling back to httpx. With neither
# installed, all nine paperbook/props tests raise RuntimeError at import —
# which reads exactly like a real test failure and is not one. This was an
# accidental dependency before: the suite passed locally only because httpx
# happened to be present in the environment, and CI, which installed only the
# two runtime requirement files, went red on a green codebase.
httpx2>=0.28
